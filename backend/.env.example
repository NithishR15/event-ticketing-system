# Server
PORT=5000
NODE_ENV=development

# MongoDB Atlas (Free Tier) - replace with your connection string
MONGO_URI=mongodb+srv://nithishuserpro_db_user:Nithishdb4@cluster0.gbnaoto.mongodb.net/event_ticketing?appName=Cluster0

# JWT
JWT_SECRET=replace_this_with_a_long_random_secret_key
JWT_EXPIRE=7d
JWT_COOKIE_EXPIRE=7

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:5173

# Nodemailer (optional - for email notifications, use Gmail App Password or Ethereal for testing)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your_email@gmail.com
SMTP_PASSWORD=your_app_password
FROM_EMAIL=noreply@eventticketing.com
FROM_NAME=Smart Event Ticketing

# App base URL (used for QR data / ticket links)
BASE_URL=http://localhost:5000
